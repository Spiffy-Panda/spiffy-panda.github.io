.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.next
build
seeds
.dendron.*
.dendron.cache.*

# Local-only in-progress workspace — never built into the site, never pushed to the public repo
content/In-Progress-Workspace/
